# DEPRECATED

This file is kept for backward compatibility with older loaders that
expect a lowercase `skill.md`.  New loaders (e.g. OpenClaw) MUST read
the uppercase `SKILL.md` at the repository root.

See `SKILL.md` for the actual skill description.
