[project]
name = "universal-image-prompt-skill"
version = "1.0.0"
description = "Portable image prompt skill with backend adapters for agents"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0.1", "requests>=2.31.0"]

[tool.black]
line-length = 100
